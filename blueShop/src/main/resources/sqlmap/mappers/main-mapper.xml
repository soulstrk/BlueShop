<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.soul.skid.main.service.impl.HomeMapper">
  	<select id="selectMainServiceList" resultType="map">
  		SELECT
  			ID
  		FROM
  			TEST
  	</select>
  </mapper>